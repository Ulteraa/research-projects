# Outputs

Generated reconstructions, model exports, point clouds, and intermediate depth files are intentionally excluded from Git because they are large.

The repository retains small JSON summaries under `results/` so the reported experiment can be audited without committing datasets or multi-megabyte point clouds.
